<Application x:Class="FTO_App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Paleta unificada: sidebar escura + header de módulo no meio-termo -->
        <SolidColorBrush x:Key="SidebarBackgroundBrush">#0f172a</SolidColorBrush>
        <SolidColorBrush x:Key="SidebarHoverBrush">#1e293b</SolidColorBrush>
        <SolidColorBrush x:Key="SidebarMutedBrush">#64748b</SolidColorBrush>
        <SolidColorBrush x:Key="LoginPrimary">#0f172a</SolidColorBrush>
        <SolidColorBrush x:Key="LoginAccent">#3b82f6</SolidColorBrush>
        <SolidColorBrush x:Key="LoginText">#334155</SolidColorBrush>
        <SolidColorBrush x:Key="LoginMuted">#94a3b8</SolidColorBrush>

        <!-- Meio-termo entre sidebar (#0f172a) e o azul antigo (#0b3d91) -->
        <SolidColorBrush x:Key="PrimaryColor">#1e3a5f</SolidColorBrush>
        <SolidColorBrush x:Key="AccentColor">#3b82f6</SolidColorBrush>

        <SolidColorBrush x:Key="WindowBackgroundBrush">#F5F5F7</SolidColorBrush>
        <SolidColorBrush x:Key="CardBackgroundBrush">#FFFFFF</SolidColorBrush>
        
        <SolidColorBrush x:Key="InputBackgroundBrush">#FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="InputTextBrush">#000000</SolidColorBrush>
        
        <SolidColorBrush x:Key="TextBrush">#000000</SolidColorBrush>
        <SolidColorBrush x:Key="SecondaryTextBrush">#555555</SolidColorBrush>
        <SolidColorBrush x:Key="TitleBrush">#1e3a5f</SolidColorBrush>
        <SolidColorBrush x:Key="BorderBrush">#CCCCCC</SolidColorBrush>
        <SolidColorBrush x:Key="PositiveBrush">#2E7D32</SolidColorBrush>
        <SolidColorBrush x:Key="NegativeBrush">#C62828</SolidColorBrush>
        <SolidColorBrush x:Key="ProfitBackgroundBrush">#F1F8E9</SolidColorBrush>

        <SolidColorBrush x:Key="GridHeaderBrush">#E8EEF5</SolidColorBrush>
        <SolidColorBrush x:Key="GridHeaderForeground">#1e3a5f</SolidColorBrush>
        <SolidColorBrush x:Key="GridAltRowBrush">#F8FAFC</SolidColorBrush>
        <SolidColorBrush x:Key="RowBackgroundBrush">#FFFFFF</SolidColorBrush>
        <SolidColorBrush x:Key="GridLinesBrush">#D0D0D0</SolidColorBrush>

        <SolidColorBrush x:Key="ActionPayBg">#E8F5E9</SolidColorBrush>
        <SolidColorBrush x:Key="ActionPayFg">#2E7D32</SolidColorBrush>
        <SolidColorBrush x:Key="ActionEditBg">#E3F2FD</SolidColorBrush>
        <SolidColorBrush x:Key="ActionEditFg">#1e3a5f</SolidColorBrush>
        <SolidColorBrush x:Key="ActionDelBg">#FFEBEE</SolidColorBrush>
        <SolidColorBrush x:Key="ActionDelFg">#C62828</SolidColorBrush>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource SidebarHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource InputTextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource InputTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="3" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource InputTextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,0"/>
            
            <Style.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource InputTextBrush}"/>
                    <Setter Property="CaretBrush" Value="{DynamicResource InputTextBrush}"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource InputTextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="CalendarStyle">
                <Setter.Value>
                    <Style TargetType="Calendar">
                        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginInputStyle" TargetType="Control">
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource LoginText}"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2d4a73"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="3" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E8EEF5"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModuleDeviceComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="320"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource LoginText}"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatusColorStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Pago}" Value="Pago">
                    <Setter Property="Foreground" Value="{DynamicResource PositiveBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Pago}" Value="Em Aberto">
                    <Setter Property="Foreground" Value="{DynamicResource NegativeBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Pago}" Value="Em execução">
                    <Setter Property="Foreground" Value="#FF6F00"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Pago}" Value="Não aprovado">
                    <Setter Property="Foreground" Value="#6A1B9A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>